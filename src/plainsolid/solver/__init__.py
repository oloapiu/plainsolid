"""Sketch constraint solver (package named solver, not sketch, so it cannot shadow the DSL function): a system of variables and constraints built from
a sketch feature, solved with scipy, with drag and degree-of-freedom analysis."""
